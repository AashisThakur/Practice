import { FC } from 'react';

const Header : FC = () => {  // ({name, isPresent} : HeaderProps) => {    // const Header: FC<HeaderProps> = ({name, isPresent}) => {
  return <>
  <h1>CRUD APP!</h1>
  </> ;
};

export default Header;